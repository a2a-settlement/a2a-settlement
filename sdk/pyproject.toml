[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "a2a-settlement"
version = "1.2.0"
description = "Settlement semantics for A2A tasks (A2A-SE) — economic commitment, delivery, verification, release/refund."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
keywords = ["a2a", "settlement", "agent-settlement", "escrow", "agent"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Software Development :: Libraries",
]
dependencies = [
  "httpx>=0.27",
]

[project.urls]
Homepage = "https://a2a-settlement.org"
Repository = "https://github.com/a2a-settlement/a2a-settlement"
Documentation = "https://docs.a2a-settlement.org"
Issues = "https://github.com/a2a-settlement/a2a-settlement/issues"

[project.optional-dependencies]
examples = [
  "a2a-sdk[http-server]>=0.3",
]
identity = [
  "PyNaCl>=1.5.0",
  "base58>=2.1.0",
]
dev = [
  "pytest>=7.0",
]

[tool.setuptools]
packages = ["a2a_settlement"]

